# 최단 경로 알고리즘 (Shortest Path Algorithm)

최단 경로 알고리즘(Shortest Path Algorithm)은 그래프에서 두 노드 사이의 가장 짧은 경로를 찾는 알고리즘이다. 경로의 길이는 간선의 가중치의 합으로 측정한다.

최단 경로 알고리즘의 특징은 다음과 같다.
1. 목적 지점까지의 최단 경로 탐색
2. 가중 그래프 : 간선에 가중치가 할당되어 경로의 길이 탐색
3. 유향 및 무향 그래프
4. 최적 부분 구조 : 큰 문제를 작은 부분 문제로 나누어 최적해의 결합을 통해 전체 최적해 탐색 
  
최단 경로 알고리즘의 종류는 다음과 같다.
- Dijkstra Algorithm
  음수 가중치를 가진 간선이 없는 그래프에서 한 노드에서 다른 모든 노드까지의 최단 경로를 찾는 방법이다.
  동작 방식 : Greedy
  1. 시작 노드를 선택하고, 해당 노드에서 직접 연결된 노드까지의 최단 경로 초기화
  2. 아직 방문하지 않은 노드 중에서 최단 경로로 갈 수 있는 노드를 선택하고, 해당 노드를 방문한 것으로 표시
  3. 선택한 노드를 경유로 다른 노드로 가는 더 짧은 경로가 존재한다면 경로 갱신
  4. 모든 노드를 방문할 때까지 위 과정 반복
- Bellman-Ford Algorithm
  음수 샂우치를 가진 간선을 포함한 그래프에서도 사용 가능한 한 노드에서 모든 노드까지의 최단 경로를 찾는 방법이다.
  동작 방식 : Dinamic Programming
  1. 시작 노드를 선택하고, 해당 노드에서 직접 연결된 노드까지의 최단 경로 초기화
  2. 아직 방문하지 않은 노드 중에서 최단 경로로 갈 수 있는 노드를 선택하고, 해당 노드를 방문한 것으로 표시
  3. 선택한 노드를 경유로 다른 노드로 가는 더 짧은 경로가 존재한다면 경로 갱신
  4. 모든 노드를 방문할 때까지 위 과정 반복
- Floyd-Warshall Algorithm
  모든 정점 쌍 간의 최단 경로를 찾을 때 사용한다. 각 정점을 경유지로 최단 경로를 계산한다.
  1. 모든 노드 쌍 사이의 최단 경로를 초기화
  2. 모든 중간 노드를 순회하면서 해당 중간 노드를 경유하는 경로가 더 짧으면 최단 경로 갱신
  3. 위 과정을 모든 노드와 중간 노드에 대해 반복

# 최단 경로
`Gold 4` `1753`
```python

```

# 특정한 최단 경로
`Gold 4` `1504`
```python

```

# 숨바꼭질 3
`Gold 5` `13549`
```python

```

# 미확인 도착지
`Gold 2` `9370`
```python

```

# 타임머신
`Gold 4` `11657`
```python

```

# 플로이드
`Gold 4` `11404`
```python

```

# 운동
`Gold 4` `1956`
```python

```
